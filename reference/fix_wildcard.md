# Replace user-friendly 'wildcard' operator with appropriate regex syntax

Replace user-friendly 'wildcard' operator with appropriate regex syntax

## Usage

``` r
fix_wildcard(x)
```

## Arguments

- x:

  Search query to be reformatted
