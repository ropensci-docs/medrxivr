# Replace user-friendly 'NEAR' operator with appropriate regex syntax

Replace user-friendly 'NEAR' operator with appropriate regex syntax

## Usage

``` r
fix_near(x)
```

## Arguments

- x:

  Search query to be reformatted
